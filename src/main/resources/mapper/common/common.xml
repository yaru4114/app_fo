<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bid.common.dao.CommonDAO" >
  <select id="selectCommList" parameterType="String" resultType="com.bid.common.model.CoCmmnCdVO">
    /* common.selectCommList */
    SELECT SUB_CODE
         , CODE_NM
		 , CODE_LT
		 , CODE_DCONE
		 , CODE_DCTWO
		 , CODE_REFRNONE
		 , CODE_REFRNTWO
      FROM dbo.CO_CMMN_CD
     WHERE 1=1
	   AND MAIN_CODE = #{mainCode}
	   AND USE_AT = 'Y'
	   AND DELETE_AT = 'N'
     ORDER BY CODE_ORDR
  </select>

</mapper>